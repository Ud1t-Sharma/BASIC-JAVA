class seventeenth{
    public static void main (String[]args){
        var a = (" HI GUYS WE WILL LEARN SOMETHING ABOUT ASSIGNMENT OPERATORS:-\nAssignment operators are used to assign values to variables.\n"); 
        int b = 7;// = OPERATOR ASSIGNS A VALUE TO A VARIABLE 
        int c = 6;// = OPERATOR ASSIGNS A VALUE TO A VARIABLE 
        int d = 8;// = OPERATOR ASSIGNS A VALUE TO A VARIABLE 
        int e = 4;// = OPERATOR ASSIGNS A VALUE TO A VARIABLE 
        int f = 68;// = OPERATOR ASSIGNS A VALUE TO A VARIABLE 
        int g = 55;// = OPERATOR ASSIGNS A VALUE TO A VARIABLE 
        int h = 41;// = OPERATOR ASSIGNS A VALUE TO A VARIABLE 
        int i = 25;// = OPERATOR ASSIGNS A VALUE TO A VARIABLE 
        int j = 20;// = OPERATOR ASSIGNS A VALUE TO A VARIABLE 
        int k = 23;// = OPERATOR ASSIGNS A VALUE TO A VARIABLE 

        b +=5;//ADDS THE GIVEN VALUE TO VAIRABLE (B += 5 :- B = B+5)
        c -=1;//DECREASES THE GIVEN VALUE FROM THE VARIABLE (B -=1 :- B = B-1)
        d *=5;//MULTIPLIES THE GIVEN VALUE WITH THE VARIABLE (D*=5 :- D = D*5)
        e /=2;//DIVIDES THE GIVEN VALUE WITH THE VARIABLE (E /=2 :- E = E/2)
        f %=3;//GIVES THE REMAINDER
        g &=5;//It returns an integer value that is the bitwise AND of the two operands
        h |=4;//It is a bitwise-OR-assignment operator. It takes the current value of the LHS, bitwise-ors the RHS, and assigns the value back to the LHS.
        i ^=2;//It takes the current value of the left-hand side (LHS), bitwise-XORs the right-hand side (RHS), and assigns the value back to the LHS.
        j >>= 5;//It takes the current value of the left-hand side (LHS), right-shifts the bits by the number of positions specified by the right-hand side (RHS), and assigns the value back to the LHS.
        k <<= 3;//It takes the current value of the left-hand side (LHS), left-shifts the bits by the number of positions specified by the right-hand side (RHS), and assigns the value back to the LHS
        
        System.out.println(a + b);
        System.out.println(c);
        System.out.println(d);
        System.out.println(e);
        System.out.println(f);
        System.out.println(g);
        System.out.println(h);
        System.out.println(i);
        System.out.println(j);
        System.out.println(k);

    }
}