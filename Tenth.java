class tenth
{
    public static void main (String[] args)
{
var name = "hello Hoomans this is the is being operated so that we can learn something. \n";
char xs = name.charAt(6);//StringName.charat() gives the index character of string. base index of string is zero
int nameLength = name.length();

System.out.println(name + xs + "\nlength of string is " + name.length() + "\ncharacters of string mentioned above are now in uppercase." + name.toUpperCase());
}// xs is a variable in which we put the value of base index whose character value is required.
}